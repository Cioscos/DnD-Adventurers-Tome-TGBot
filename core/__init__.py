"""Shared core package — code used by both the Telegram bot (``bot/``) and
the FastAPI backend (``api/``).

Contains the SQLAlchemy models and engine, static D&D 5e data tables, and
utility helpers that are not tied to any single deployment surface.
"""
