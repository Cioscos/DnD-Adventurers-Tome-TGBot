# Ledger — impeccable pass per componenti

Coda di lavoro a batch di pagina. Stati: `☐` da fare · `◐` parziale · `✅` completato · `⛔` bloccato (motivo).
Questo ledger è stato seminato dall'audit FE del 2026-06-11 (`fe-playwright-audit/reports/2026-06-11_18-58-mobile/`): i findings citati (#n) vengono da quel `report.md`. Le run della skill risolvono comunque l'audit PIÙ RECENTE disponibile (vedi SKILL.md, Passo 0): se nel frattempo ne esiste uno più nuovo, integra qui i suoi findings prima di lavorare il batch.

> Baseline impeccable audit: **14/20** (2026-06-11, pre-B0). Score di fine giro: aggiornare qui.
> Sessione residui (2026-06-12, post-coda): **19/20** a composizione diversa — Perf **3→4** (finalmente misurata: polling SessionRoom 24→0 commit idle/min, DiceScene e route sane, vedi R2), Acc **4→3** (lo smoke device R3 ha rivelato che il back di sistema NON chiude gli overlay su Telegram fullscreen: minimizza la app; fix futuro via BackButton API, vedi Fuori scope), Theming 4, Responsive 4, Anti-Patterns 4 (le 3 divergenze DESIGN.md↔codice DECISE e benedette in R1: non sono più divergenze).
> Fine giro r6 (B14-B16, CODA COMPLETA): **19/20** — Acc 4 (touch target ≥40/44 e ESC/back su TUTTI gli overlay, ultimi 4 one-off di session migrati a Sheet; conferme su ogni distruttiva; 11px minimo sulle label), Perf 3 (mai profilata in nessun giro: salire richiede misure, vedi B16), Theming 4 (ban #000/#fff e arbitrary var() a ZERO app-wide, verificato a grep), Responsive 4 (375+320 su homebrew e sessione), Anti-Patterns 4 (one-off estinti; restano solo divergenze DESIGN.md↔codice da benedire in `document`, annotate in B16). Delta dalla baseline: **+5 in 6 giri**.
> Fine giro r2 (B1-B3): **18/20** — Acc 4 (contrasto chip e touch target risolti; 12px di sistema ok), Perf 3 (invariata, nessun profiling), Theming 4, Responsive 3 (#V1 nome header ancora aperto → B4), Anti-Patterns 4 (side-stripe e em dash eliminati; resta z-[9999] in Maps, P3 → B11). Re-audit completo in B16.
> Fine giro r4 (B8-B10): **19/20** — Acc 4 (touch target editor/contatori a 44px, conferme su tutte le azioni distruttive), Perf 3 (invariata, nessun profiling; -300 righe di codice morto), Theming 4 (#000/#fff eliminati da Button arcane/SpellDamageSheet/LevelUpBanner; restano text-black/white in Dice/Conditions/Maps/Session → B11/B12/B15), Responsive 4 (invariata), Anti-Patterns 4 (4 footer invertiti raddrizzati, 3 violazioni Semantic Triad rimosse, ESC/back ora su tutti gli overlay dei batch lavorati; restano z-[9999] Maps → B11 e i one-off di session → B15). Re-audit completo in B16.
> Fine giro r5 (B11-B13): **19/20** — Acc 4 (44px su note/zone/chip-remove, mic spiegato al tap), Perf 3 (invariata, nessun profiling; bundle più snello: -4 file morti/legacy), Theming 4 (ban #fff completato sui residui noti: ripple Button, pip spossatezza, badge FAB, viewer mappe; ombre warm; restano i one-off di session → B15), Responsive 4 (320 verificato sui nuovi batch), Anti-Patterns 4 (z-[9999] eliminato — ultimo P3 noto; footer invertiti azzerati; B14/B15 ancora non auditati). Re-audit completo resta in B16 dopo B14-B15.
> Fine giro r3 (B4-B7): **19/20** — Acc 4 (dots 40px, header 40px, long-press con ring di progresso), Perf 3 (invariata; color-mix a costo trascurabile, nessun profiling), Theming 4 (fix alpha: 312 tinte reali in entrambi i temi, #fff banditi; resta la divergenza DESIGN.md↔codice su font-display/spring.elastic → B16/document), Responsive 4 (#V1 risolto, verificato 375+320), Anti-Patterns 4 (copy hardcoded i18n-izzato; resta z-[9999] Maps → B11). Re-audit completo in B16.
> B0 (già consegnato in PR #166): label stats, pill On/Off, pb-24 swiper, side-stripe saves/combatant, backdrop warm, chip % HP, icona velocità.

## Batch trasversali (prima: sbloccano i batch di pagina)

- ✅ **B1 — Modali & overlay** · `ui/Sheet`, `ui/ConfirmSheet`, `ui/ResultDialog`, `ModalProvider`, `hp/HitDiceModal`, `notes` delete confirm, `maps/MapUploadForm`
  Findings: #4 ordine bottoni invertito (HitDiceModal, conferma nota, MapUploadForm → footer condiviso conferma-a-destra), #7 ESC non chiude ResultDialog, #12 back/history non chiude gli overlay (integrazione popstate/BackButton in ModalProvider).
  Esito: nuovo hook condiviso `hooks/useOverlayDismiss` (stack globale ESC + sentinella history singola, StrictMode-safe) agganciato a Sheet/ResultDialog/ModalProvider → copre anche SearchOverlay e tutti gli sheet derivati. Footer invertiti in HitDiceModal (+label "Riposa" anti-wrap), MapUploadForm; conferma nota migrata a ConfirmSheet (one-off rimosso); fallback `cancelLabel` di ConfirmSheet ora i18n; X di Sheet 30→40px. Verificato 375×667 dark+light, ESC annidato (dialogo sopra sheet), back su Sheet/ResultDialog/SearchOverlay, vitest overlay, tsc+lint. **Da verificare su device Telegram**: comportamento BackButton nativo con la sentinella history.
  Nota per i batch successivi: gli overlay CUSTOM non-Sheet (`EquipItemPicker`, `ItemDetailsModal`, `ProgressionFullTableModal`, `SlotActionSheet`, viewer di `Maps`, `AddClassForm`, `LevelUpModal`, `EditClassesModal`) vanno agganciati a `useOverlayDismiss` nei rispettivi batch (B4/B5/B10/B11).
- ✅ **B2 — Copy & i18n** · `locales/it.json`, `locales/en.json`, formatter numerici/orari
  Findings: #1 em dash (sweep U+2014 su entrambi i locale, incl. "Salta — compili dopo" e HandsConflictDialog), #8 "intelligence" grezzo nel modale save, #V4 separatore migliaia ("2,700"→"2.700" in it) e orario AM/PM nel feed sessione (→24h, coerente con Cronologia). Centralizzare `Intl.NumberFormat(locale)`/`hour12:false`.
  Esito: em dash rimossi dal copy (identity_skip/optional_hint, HandsConflictDialog body, private_hint fallback, CombatantSheet label→":", SessionFeed separatore→"·"); i "—" segnaposto valore-assente restano (glifo, non copy). #8: description raw soppressa nei saves (titolo già localizzato). #V4: nuovo `lib/format.ts` (formatInt/formatTime24/localeTag sul locale APP, non navigator) adottato da HeroXPBar, Experience (incl. locale di AnimatedNumber) e SessionFeed. Nota: it-IT CLDR non raggruppa i numeri a 4 cifre ("2700", "27.000" da 5 in su) — corretto, non regressione. Verificato: wizard step 3, save INT, hero XP, feed 24h, tsc+lint.
- ✅ **B3 — Stati di errore & harden** · `AppErrorBoundary`, hook dati condiviso, `hp/DeathSaves`
  Findings: #10 id inesistente → main vuoto (EmptyState "Personaggio non trovato" + CTA lista nel Layout su 404), #5 indicatore "Stabilizzato · privo di sensi" (cobalt + icona) a 0 HP stabile. Passata empty/loading/error sulle pagine principali.
  Esito: #10 guard condiviso in `Layout` (stessa query del personaggio → dedupe; 404/403 = EmptyState "non trovato" + CTA lista, altri errori = EmptyState + Riprova; retry disattivato sui 404). Copre tutte le pagine Layout-based in un punto solo; CharacterMain ha già il suo branch. #5: badge cobalt `stabilized_badge` (HeartPulse + Cinzel) nella sezione alive quando `hp=0 && stable`. Verificato: /char/9999/hp + CTA, danno→0→stable→badge→cura, tsc+lint.

## Batch di pagina

- ✅ **B4 — Hub personaggio** · `CharacterMain`, `character/HeroScreen`, `CharacterSwiper`, `SwiperDots`, `HeroStatsSection`, `QuickActions`, `SpellSlotsSummary`, `ProgressionPreview`, `ProgressionFullTableModal` (⛔ mai aperto: esercitalo), `ui/HPGauge`, `ui/HeroXPBar`, `ui/ConditionBadge`, `SearchOverlay` (#11: indicizzare risorse homebrew)
  Esito: #11 risolto (`characterSearch` accetta `HomebrewResource[]`, SearchOverlay le fetcha a sheet aperto con la stessa query key di Abilità, gruppo "Risorse" con GiCrystalBall e contatore `current/max`; placeholder aggiornato; +3 test). #V1 risolto (header: controlli 40px, gap/padding compatti, pill ispirazione impilato icona+On/Off da 42px → nome 113→165px a 375, ~9 char anche a 320). ProgressionFullTableModal esercitato: agganciato `useOverlayDismiss` (ESC+back verificati), intestazioni tabella abbreviate ("Liv."/"Comp.") e celle `L{n}` — niente più spezzature a metà parola, 12 righe visibili. Polish: error state di CharacterMain allineato al guard del Layout (EmptyState 404 + CTA lista / Riprova, stessa retry policy), SwiperDots con touch target 40×40 (pallino visivo invariato, wrapper pointer-events-none) e token al posto dei colori hardcoded, micro-cleanup (`truncate`+`break-words`, `bg-dnd-ink/25`). Manutenzione test r2: mock `ApiError`/`useDragControls` in CharacterMain/CombatPanel/Layout test, `retryDelay: 0` in renderWithProviders. Verificato 375×667 + 320×568, dark+light, vitest/tsc/lint verdi. Conforme (non toccato): label "Skills" bilingue deliberata; AC in font-display nello scudo (inscription, non numero tabellare).
- ✅ **B5 — PaperDoll & equip** · `character/EquipmentScreen`, `PaperDoll`, `EquipmentSlotCell`, `SlotActionSheet`, `EquipItemPicker`, `EquipmentStatsFooter`, `HandsConflictDialog`, `ItemDetailsModal` (⛔ mai aperto), silhouette upload
  Esito: i 3 overlay custom (SlotActionSheet, EquipItemPicker, ItemDetailsModal) agganciati a `useOverlayDismiss` — ESC/back verificati incluso lo stack annidato picker→HandsConflictDialog. ItemDetailsModal esercitato (Cuoio Audit): X 36→40px, valori numerici in mono (Tabular Numerics). Picker: tipo oggetto tradotto ("weapon"→"Arma"); HandsConflictDialog spostato fuori da AnimatePresence (warning React di chiavi duplicate eliminato). Conforme: HandsConflictDialog (annulla sx / continua dx), EquipmentStatsFooter (icona velocità già muted da B0), celle slot 46-56px con halo-equip come segnale. Verificato 375×667 dark, console pulita, vitest character (73)/tsc/lint verdi.
- ✅ **B6 — Punti Ferita** · `pages/HP`, `hp/HpOperationForm`, `DeathSaves`, `DeathSaveResultDialog`, `InstantDeathDialog`, `DeadState`, `HitDiceResultDialog`, `ConcentrationSaveDialog`, `components/HPBar` (light mode con rigore pari al dark)
  Esito: **fix di sistema (trasversale)** — i modificatori alpha sui token (`bg-dnd-gold/15` ecc., 312 usi) erano no-op silenziosi: Tailwind non inietta alpha in un colore `var()`. Config migrato a `color-mix(<alpha-value>)` (Chrome/WebView 111+, Safari 16.2+) + sweep delle 55 classi arbitrary `x-[var(--dnd-N)]/A` → `x-dnd-N/A` in 23 file: tutte le tinte selected/semantic previste dal design ora esistono davvero (pip morte, bottoni manuali, pill ON, righe progressione, ...). Batch: i18n dei banner critico/fumble (3 modali, chiavi `character.dice.{critical,fumble}_banner`) e CD concentrazione (`conc_save_title`, "CD" in it); ban #fff (`text-white`→`text-dnd-parchment` in DeathSaves/HpOperationForm); chip % HP theme-aware (`bg-dnd-ink/25`, chiude #V3 in radice). Stati esercitati: vivo (dark+light), morente (pip+tiro d20+dialogo), stabilizzato. Conforme: DeadState/InstantDeathDialog/HitDiceResultDialog/ConcentrationSaveDialog (ResultDialog standard). Nota sistemica per B16/document: `spring.elastic` (pop dei valori, 10+ file) e `font-display` sui numeri eroe divergono da DESIGN.md §Motion/§Hero Numbers — decidere se benedirli nel doc o sostituirli, non è un fix da batch.
- ✅ **B7 — Tiri** · `pages/Skills`, `pages/SavingThrows`, `pages/Actions`, `RollResultModal`, `WeaponAttackModal`, `InspirationRerollButton`, long-press competenza (`useLongPress`)
  Esito: batch in gran parte conforme (le pagine erano già state rifinite in B0/B2; i banner critico/fumble erano stati sistemati in B6). Fix: totale negativo in crimson anche nei Saves (come in Skills), `ts_short` accorciato a "comp"/"prof" (la sub-riga leggeva "+1 +6 Tiro Salvezza"). Esercitati: long-press → ring di progresso → picker competenza con breakdown, tiro TS con nat 20 (banner CRITICO! i18n, breakdown mono, USA ISPIRAZIONE col gradiente arcane ora reale post-fix alpha). Beneficiari del fix alpha di B6: riga expert (velo arcane→gold) e InspirationRerollButton, prima senza gradiente. Conforme: Actions (unarmed+armi), useLongPress (guard wasActive già in place).
- ✅ **B8 — Magia** · `pages/Spells`, `spells/SpellForm`, `SpellItem`, `SpellFilter`, `CastSpellModal`, `SpellDamageSheet` (⛔: serve spell con danno, creala), `pages/SpellSlots`, `character/AutoModeBanner`, banner concentrazione
  Esito: #9 risolto — "Dimentica" ora passa da ConfirmSheet danger (body col nome, annulla/conferma testati, DELETE solo a conferma); stessa protezione sulla X di rimozione livello in SpellSlots (title/body i18n + aria "Rimuovi slot di livello N" al posto di "Remove" hardcoded). SpellDamageSheet esercitato (⛔ chiuso, flusso completo: SRD autofill "Mani Brucianti" → cast slot 1 → tiro 3d6): bottone Rolla one-off con `text-black` → Button primary (ban #000), input dadi extra raw → `ui/Input`, checkbox critico 16px → SwitchToggle. SpellForm: checkbox nativi concentrazione/rituale → SwitchToggle (coerenti con "Infligge danno"), 3 placeholder hardcoded → i18n it+en. Fix trasversale: variante arcane di `ui/Button` aveva `text-white` → `text-dnd-parchment` (ban #fff, beneficia "Ripristina tutti" e ogni bottone arcane). CastSpellModal: conteggi slot in mono tabulare. Sweep `text-[var(--dnd-*)]` → token in 4 file (testi danger → crimson-bright: contrasto penombra del menu Dimentica e stop concentrazione). AutoModeBanner: scorciatoia impostazioni rounded-full → rounded-lg (niente pill). TotalEditor: aria decrement/increment_total i18n. Verificato 375×667 dark+light, ESC sui ConfirmSheet, console pulita, vitest spell (53)/tsc/lint verdi. Conforme: ConcentrationPanel (halo arcane pulsante = segnale legittimo), SpellFilter, sticky -top-4.
  Nota per B11/B12/B15: `text-black`/`text-white` residui fuori batch — DiceOverlay (badge contatore), Conditions (chip gradient-ember), Maps (viewer fullscreen), AddMonsterSheet e GrantItemModal (bottoni one-off con `text-black`, candidati a Button primary come fatto qui).
- ✅ **B9 — Zaino** · `pages/Inventory`, `inventory/ItemForm`, `DamageDiceBuilder`, `InventoryItem`, `AbilityModifiersEditor` (⛔ visto ma non compilato), `EffectsEditor` (⛔), `pages/Currency`
  Esito: 2 footer invertiti corretti — eliminazione oggetto (one-off Sheet con Elimina a SINISTRA → ConfirmSheet condiviso) e sheet conversione valuta (Converti/Annulla scambiati). Semantic Triad: bottone "Converti" arcane → secondary (la conversione monete non è magia). ⛔ chiusi: EffectsEditor e AbilityModifiersEditor compilati via wizard (consumabile con effetto cura + modificatore FOR +2, submit e delete OK); aggiunto focus gold sugli input raw degli editor, min-h-44 sui bottoni "Aggiungi effetto/modificatore" (~26px prima), `common.remove` promosso a chiave vera (era defaultValue). InventoryItem: "Effetto" hardcoded → `effect_label` i18n, aria stepper quantità "-"/"+" → chiavi dedicate. ItemForm: checkbox nativo svantaggio furtività → SwitchToggle (verificato su Cuoio Audit). Sweep `text-[var(--dnd-crimson-bright)]` → token (4 file). Rimosso `<AnimatePresence />` orfano in Inventory. Verificato 375×667 dark, console pulita, vitest inventory/currency (28)/tsc/lint verdi. Conforme: DamageDiceBuilder (stepper 44px, preview), wizard 3 step con WizardFooter, pill "Aggiungi" flottante su scroll (pattern deliberato), CoinTile, slot picker (set di slot tutti pari: niente caso col-span-2).
- ✅ **B10 — Crescita** · `pages/Multiclass`, `multiclass/EditClassesModal`, `AddClassForm`, `LevelUpBanner`, `LevelUpModal` (⛔: esercitarlo con multiclasse), `pages/Experience`, `pages/Abilities`, `abilities/PassiveAbilityDetailModal` (⛔: serve una passiva, creala), `homebrew/CustomResourceCounter`
  Esito: ESC/back agganciati (nota B1) a EditClassesModal e LevelUpModal via `useOverlayDismiss`; il picker annidato registra una seconda entry → ESC chiude prima il picker poi il modale (verificato). **AddClassForm eliminato: era codice morto** (nessun import; il picker vive in EditClassesModal) — rimosso col suo test, commenti aggiornati. Footer invertito nel wizard abilità step 1 (Avanti sx → dx). Picker classi: Surface arcane decorativa → tome e bottone Aggiungi arcane → primary (Semantic Triad), Annulla danger → ghost, input raw → `ui/Input`. LevelUpModal esercitato a XP 2700 (⛔ chiuso): "Liv" hardcoded ×2 → riuso `progression.level_header`; verificati preview sblocchi/badge/chips classe e ESC. Abilities: checkbox nativo Passiva → SwitchToggle (icona Sparkles), creata passiva → PassiveAbilityDetailModal esercitato (⛔ chiuso, Sheet standard). LevelUpBanner: ambra arbitrary → token e rimosso hover color-mix verso white (ban #fff adiacente). CustomResourceCounter: token ×2 e chip Recupera min-h-44 (~30px prima). Sweep token in Multiclass/Abilities/Experience; aria "Remove" → `common.remove` + nome classe. Verificato 375×667 dark, console pulita, vitest crescita (35)/tsc/lint verdi. Fixture ripristinata (XP 900, passiva di prova rimossa).
- ✅ **B11 — Stato & diario** · `pages/Conditions`, `conditions/ConditionDetailModal`, `pages/History`, `pages/Notes`, `notes/NoteEditor`, `NoteItem`, `NoteViewModal` (⛔), `VoiceRecorder` (⛔: serve permesso mic, prova su device), `pages/Maps`, `maps/MapUploadForm`, `MapZoneGroup`, `ZoomableImage`
  Esito: **fork legacy del design system eliminato** — `Card`/`DndButton`/`DndInput` (alias di Surface/Button/Input) vivevano solo in questo batch: 4 file migrati ai canonici `ui/*`, alias cancellati insieme a `StubPage` (morto, pre-design-system) e ai loro test. Trasversali: ripple di `ui/Button` `bg-white/30`→`parchment/30` e aria X di `ui/Sheet` "Close"→`common.close` (ban #fff / i18n a beneficio di tutta l'app). History: conferma svuota era invertita → ConfirmSheet (chiude #4 per quest'area), ricerca → `ui/Input` con icona+clear, rail timeline tokenizzata (il filo di luce era `rgba(255,240,200)` quasi-bianco e un oro fuori palette → gold-glow/gold-bright via color-mix, verificata in light). Conditions: `text-white` sul pip spossatezza attivo → parchment, sweep token amber/crimson (+`eventMeta.ts`). Notes: empty state "Nessuno"→"Nessuna nota" (chiave nuova), Modifica/Elimina da link 24px → icon button 44px, 🎤→icona Mic, chiavi date corrette (`common.date.*`), NoteViewModal senza "Chiudi" ridondante (Elimina sx / Modifica dx), editor e recorder a sistema (Input textarea, label Cinzel), mic non disponibile → bottone tappabile con toast esplicativo (i tooltip non esistono su mobile). Maps: viewer con `useOverlayDismiss` (ESC+back verificati), `z-[9999]`→`z-50`, backdrop freddo hardcoded → `bg-dnd-ink/95`, `text-white`/`bg-white` → parchment, upload form a sistema (FilterChip per le zone al posto di chip raw 26px, X file 44px, "Upload failed" → i18n `upload_error`), azioni zona 44px con X/FileText icone, `window.scrollTo` no-op (il body non scrolla) → `scrollIntoView` del form. **Bug preesistente fixato in ZoomableImage**: flex-centering sommato al translate della libreria raddoppiava l'offset (immagine in basso a destra) → ora centrata (`centerOnInit`, niente flex). Esercitati i ⛔ NoteViewModal/NoteEditor/MapUploadForm/viewer; **VoiceRecorder: UI esercitata in browser, la registrazione vera resta da provare su device (mic)**. Rimandato: `<audio controls>` nativo non a tema (raro, costoso da sostituire). Verificato 375×667 + 320×568, dark+light, console pulita, fixture ripulita, vitest 147 file verdi, tsc+lint ok.
- ✅ **B12 — Dadi** · `pages/Dice`, `pages/DiceStats`, `DiceOverlay` (FAB), `DicePoolResultModal`, `ui/DiceIcon`, `ui/PresetTextField`, pack texture (settings); unico posto con easing elastico ammesso
  Esito: batch già in buona forma (B0/B1 lo avevano sfiorato). DiceOverlay: badge contatore `text-white`→parchment (nota B8 chiusa), FAB e pill Lancia migrati dal gradiente arbitrary `from-gold-deep to-gold-bright` al token `bg-gradient-gold` (stesso vocabolario dei Button primary). Dice: conferma svuota-cronologia one-off → ConfirmSheet, "Oggi/Ieri" hardcoded → `common.date.*`, defaultValue rimossi (chiavi già nei locale), link Statistiche 36→44px. DicePoolResultModal: "Totale:" hardcoded → `common.total` (chiave nuova it+en). DiceStats: harden istogramma — sopra le 20 facce i tick mostrano solo i multipli di 10 (un d100 produceva 100 etichette da 7px accavallate); per il resto conforme (ConfirmSheet già a posto, mono tabulari, emerald/crimson semantici su nat20/nat1). **Nota B4 chiusa come mitigata**: a 320×568 il FAB convive col fondo pagina grazie a pb-24 + dim-on-scroll + hide-quando-overlay; nessun riposizionamento necessario. Conformi: PresetTextField (chips 44px, label Inscription), DiceIcon, spring.elastic legittimo (è il dice-roller). Esercitati: pool dal FAB (badge 1/2, Lancia, ResultDialog con totale i18n), DiceStats con dati reali. Verificato 375×667 dark, console pulita, vitest DicePoolResultModal, tsc+lint ok.
- ✅ **B13 — Identità, impostazioni & home** · `pages/Identity`, `pages/Settings` (tutte le sezioni), `pages/Changelog`, `pages/CharacterSelect`, `ui/SwitchToggle`, `ui/SelectSheet`, `ui/ChipInput` (⛔ campo Lingue mai esercitato), `ui/Flags`
  Esito: **bug i18n reale chiuso** — le sezioni "Fisicità"/"Cultura" di Identity esistevano solo come defaultValue italiano (in EN compariva l'italiano): chiavi aggiunte it+en e sweep di TUTTI i defaultValue del batch (Identity, Settings, CharacterSelect). Semantic Triad: banner "modifiche non salvate" da Surface `ember` (= crisi HP, halo-danger) a elevated con bordo **ambra** (Candle Amber = avviso). Identity: placeholder hardcoded "Comune, Elfico..."/"Armature leggere..." → chiavi esistenti `language/proficiency_placeholder`, token cobalt/crimson nei DMG_TONES. Settings: 4 arbitrary crimson → token, ombra nero-pura del D20Thumb → warm (26,16,8), input tetto preparazione raw → `ui/Input` numerico. CharacterSelect: 4 arbitrary → token, aria "Elimina" hardcoded → `common.delete`, placeholder "Artificer..." → `custom_class_placeholder` (nuova chiave); `telegramConfirm` per l'eliminazione personaggio LASCIATO deliberatamente (pattern nativo Telegram, PRODUCT §4, usato anche in session ×4 — uniformarlo a ConfirmSheet andrebbe valutato in B15, non qui). ChipInput: aria "Remove X" hardcoded → i18n (⛔ Lingue chiuso: chips, suggerimento "Silvano", rimozione esercitati). SwitchToggle: glow rgba fuori palette → `var(--dnd-gold-glow)`. Conformi: Changelog (collapse con parchment curve), SelectSheet, Flags (colori bandiera = artefatti reali, esenti dal ban). Verificato 375×667 dark, console pulita, fixture intatta (chip rimosso senza salvare), vitest 4 file, tsc+lint ok.
- ✅ **B14 — Homebrew** · `pages/Homebrew` (hub/template), `homebrew/RuleEditor` + tutte le sections (`PropertyFormModal`, `PassiveModifierFormModal`, `EffectFormModal`, `EffectChainEditor` ⛔ mai aperti: crea una regola completa), `PropertyBadge`/`CustomConditionCard`/`HomebrewBreakdownRow`/`HomebrewNotification` (⛔: installare Sanguinamento e Qualità&Usura per vederli)
  Esito: **bug i18n locale chiuso** — le liste editor mostravano sempre `label_i18n.it` (in EN comparivano le etichette italiane): nuovo helper `resolveLabelI18n` in `lib/homebrew/i18n-dsl.ts` (locale → it → en → chiave) adottato in PropertiesSection/PassiveModifiersSection/TablesSection (7 punti, incluse `value_labels_i18n` delle righe tabella). Tutti gli aria EN hardcoded → i18n (Edit/Delete/Remove column/Delete trigger/Remove filter/Delete case → `common.*`; Move up/down → nuove chiavi `homebrew.effects.move_{up,down}` it+en). Touch target: matita/cestino di PropertiesSection 28→44px (allineati a PassiveModifiersSection), cestino tabella 40→44, X colonna e X chip-filtro → `hit-44`. FilterPicker a sistema: 2 input raw → `ui/Input`, 4 bottoni op/sì-no (forme miste rounded-lg/full) → `FilterChip` uniformi. `was_critical` checkbox nativo → SwitchToggle. 4 arbitrary `text-[var(--dnd-crimson-bright)]` → token. Error state hub: Surface ember one-off → EmptyState + Riprova (pattern B3, refetch mirato). Copy: titolo "Nuovo passo" ora reale (la chiave `modal_title_new` non era mai usata — prop `isNew` da EffectChainEditor). Label 10px→11px (badge DA TEMPLATE, radio soggetto, badge tipo, chips enum, branch label, header tabella). ⛔ tutti chiusi live: PropertyFormModal (Qualità, enum+default+footer), PassiveModifierFormModal (Talismano Audit +1 CA creato e salvato), EffectFormModal (roll_dice, damage con SwitchToggle, notify con chips severità), ActionPicker/FilterPicker, PropertyBadge (Spada corta: "Qualità: Ordinaria" neutral / "Stato: Integro" emerald), CustomConditionCard (sanguinamento via PATCH conditions, poi rimossa), HomebrewBreakdownRow ("HOMEBREW +1" sotto lo scudo CA, 15→16). Benedetti con nota per B16: i 6 `<select>` nativi (stilati a sistema; homebrew escluso da PR #154 deliberatamente, picker OS adeguato per l'editor power-user), emoji del vocabolario DSL (coerenti con eventLabel/actionLabel), FilterChip rounded-full+halo selected (de facto di sistema, diverge da DESIGN.md §Chips → decidere in `document`). Verificato 375×667 dark+light, 320×568 (griglia tabelle a scroll interno ok), ESC su tutti gli sheet, console pulita, fixture ripristinata (regole eliminate, CA 15), tsc/lint/vitest verdi. **Gotcha nuovo: Vite su /mnt/c non riceve file-event → riavviare Vite dopo ogni edit prima di verificare in browser (HMR muto, moduli JSON stantii).**
- ✅ **B15 — Sessione** · `pages/Session`, `SessionJoin`, `SessionRoom`, `session/SessionFeed`, `GrantItemModal`, `EncounterCreateSheet`, `CombatPanel`+`CombatantSheet`+`AddMonsterSheet`+`InitiativeCta`+`TurnBar`+`CombatantRow`+`RewardPopup`+`ParticipantIdentitySheet` (⛔: avviare un combattimento con 2 tab `?dev_user`), `ui/InSessionBanner`
  Esito: **regressione B3 trovata e corretta in Layout** — il guard 404 trattava QUALSIASI param `:id` come personaggio: su `/session/:id` un giocatore vedeva la stanza oscurata da un finto "Personaggio non trovato" (403 su /characters/<sessionId>); ora il guard è attivo solo su `pathname.startsWith('/char/')` (+ mock useLocation nel test Layout). **Harden note r5 chiuso**: Session.tsx:64 crashava su `participants` undefined E mostrava sempre "Game Master" (confrontava gm_user_id col GM stesso); ora `api.me()` + `getMyRole` null-safe — il ruolo del viewer è reale (verificato GM e Giocatore nei 2 tab); stesso guard `participants?.` in InSessionBanner e SessionRoom. **3 bottom-sheet one-off migrati a `ui/Sheet`** (ESC/back/X 40px/overlay warm in un colpo, chiude la nota B1 per l'area): EncounterCreateSheet, AddMonsterSheet (input raw ×5 → `ui/Input`, footer → Button secondary/primary: via il `text-black` nota B8, + label "Nome" dedicata invece del titolo duplicato), CombatantSheet (input → ui/Input, condizioni/azioni min-h-44, aria-pressed). GrantItemModal step destinatari → Sheet + righe toggle con Check custom (via checkbox nativi e secondo `text-black`) + footer Button. RewardPopup: ESC manuale → `useOverlayDismiss` (guadagna il BackButton), spring inline → `spring.swipe`, defaultValue ×3 rimossi. SessionFeed: fullscreen su `useOverlayDismiss`, X fullscreen e Maximize 26-34px → 40px, X sussurro → icona + `hit-44`, input chat 37→44px con focus gold, "carica precedenti" 40px. Sweep arbitrary `[var(--dnd-*)]` → token (36 in 6 file), `bg-black` bucket dead → `bg-dnd-ink`, `bg-black/60` ×3 → overlay di sistema, glow riga attiva rgba → `shadow-dnd-glow`. **telegramConfirm BENEDETTO** come pattern nativo per i flussi rapidi GM/azioni di uscita (PRODUCT §4; 4 usi in session + 1 in CharacterSelect): su client moderni è il popup nativo Telegram, in browser fallback window.confirm. ⛔ combat chiuso live con 2 tab (?dev_user=777): incontro full creato, 2 goblin aggiunti, iniziativa player con d20 (1!), avvio con auto-roll (telegramConfirm), TurnBar round 1→2, skip PG via ConfirmSheet (conferma a destra), turno player-driven "Termina turno", danno+condizione da CombatantSheet (4/7 + Avvelenato, redatto a fascia ambra per il player), segna morto, consegna oggetto → RewardPopup sul tab player (ESC ok), sussurro+annulla, feed fullscreen, termina incontro. Verificato 375×667 dark+light + 320×568, console pulita (solo 409 gestito e fallback showPopup), fixture ripristinata (item rimosso, incontro chiuso), tsc/lint e vitest FULL (147 file/696 test) verdi. Fuori scope ma annotato: con 2 tab dev sullo stesso browser il localStorage condiviso (activeCharId) genera 403 innocui.

## Chiusura giro

- ✅ **B16 — Re-audit** · ricalcola lo score impeccable (5 dimensioni), aggiorna la baseline qui sopra, riepilogo delta per l'utente. Se <17/20, apri il giro successivo con i residui.
  Esito: score **19/20** (vedi header). Chiusura con sweep finale evidence-based: gli ultimi 49 arbitrary `[var(--dnd-*)]` (21 file: HeroScreen, DeathSaves, Input, StatPill, MenuScreen, ...) → token; ultimo `text-black` (badge DEV #id) → `text-dnd-ink`; verifica grep a ZERO su #000/#fff/black/white di classe, arbitrary var, z-[9*]; em dash nei locale = solo il glifo valore-assente `ac_hidden`. Smoke post-sweep su /hp (hero number, semantici) + vitest mirato (84) e tsc/lint verdi. **La coda del ledger è completa: tutti i batch B0-B16 lavorati.** Residui consegnati al futuro (non aprono un giro: score ≥17): ① divergenze DESIGN.md↔codice da DECIDERE in `$impeccable document` — `font-display` (Cormorant) usato sui titoli di card/liste e numeri eroe oltre il ruolo §Display, `spring.elastic` nei pop dei valori (10+ file) vs §Motion, `FilterChip` rounded-full+halo-selected vs §Chips rounded-lg (de facto standard in tutta l'app); ② verifiche solo-device: VoiceRecorder (mic), BackButton Telegram con la sentinella history di useOverlayDismiss, telegramConfirm con showPopup nativo; ③ Perf resta 3/4: mai profilato — un eventuale giro `optimize` parta da misure (re-render del polling 2.5s in SessionRoom, peso DiceScene).

## Residui B16 (sessione dedicata 2026-06-12, branch `chore/design-pass-residui`)

- ✅ **R1 — `$impeccable document`** · Le 3 divergenze DESIGN.md↔codice DECISE: tutte benedette nel doc con dottrine a confini stretti (il codice era coerente app-wide, il doc indietro). ① `font-display` → nuova **Illuminated Capital Rule**: Cormorant Unicase per nomi propri e il SINGOLO valore eroe della superficie (HP 5rem, CA, punteggio); i compagni comparativi (`/max`, breakdown, colonne) restano mono; §Hero Numbers riallineato al codice (era "set in mono", falso). ② `spring.elastic` sanzionato per dice roller + value-pop di singolo elemento alla rivelazione di un risultato (i 14 usi reali sono tutti così); resta vietato su layout/liste/pagine/modali; aggiunti `spring-snappy`/`spring-elastic` al sidecar motion. ③ FilterChip benedetto come SECONDA SPECIE di chip (filter pill `rounded-full` 44px: la forma segnala l'interattività; la losanga resta etichetta statica); **Halo-as-Signal sdoppiata** in dialetto attenzione (max 1/schermo) e dialetto selezione (ripetibile su una riga di filtri); token `chip-filter`/`chip-filter-selected` nel frontmatter + componente Filter Pill nel sidecar.
- ✅ **R2 — Giro Perf (`$impeccable optimize`), SOLO da misure** · Strumentazione: hook `__REACT_DEVTOOLS_GLOBAL_HOOK__` minimale (conteggio commit + `actualDuration`), PerformanceObserver longtask, fetch logger, rAF sampler; Vite dev su 5173, API locale, 375×667.
  **SessionRoom (polling 2,5s)** — BEFORE: 24 commit React/60s = 1 per OGNI poll di `/sessions/6/live` (~3,4ms avg, 81,7ms/min di scripting, 0 longtask; ~1.440 re-render/ora dell'intera stanza a sessione ferma). Causa: `_touch(session)` server-side aggiorna `last_activity_at` a ogni GET e il campo (mai letto dal FE) rompe lo structural sharing. FIX: stripping del campo nel `queryFn` prima della cache + `last_activity_at?` nel tipo (`SessionRoom.tsx`, `types/index.ts`). AFTER: **0 commit/60s** a parità di rete (24 live + 20 feed); sanity check: danno via API → row `17/22` entro un tick (6 commit), cura → `22/22`, fixture ripristinata. Il feed (poll 3s, merge con dedup) era già a 0 commit di suo.
  **DiceScene** — mount 34ms (chunk 1.010KB/281KB gz lazy + preload in idle: funziona), `frameloop="demand"` (a riposo non renderizza), scena tenuta montata deliberatamente (evita ~0,9s di re-init shader a ogni tiro), fisica JS ~0 longtask a regime. FPS qui NON rappresentativi (WebGL = SwiftShader software in WSL: 17fps cold/38 warm sono il floor CPU-only, su device c'è la GPU). **Nessun fix giustificato dalle misure.** Eventuale verifica FPS solo su device reale.
  **Pagina più pesante** — mount a confronto (dev build, reload freddo): hub `/char/9` 68,4ms totali/commit max 53ms (monta 3 schermate dello swiper), tutte le altre 22-38ms, 0 longtask ovunque. **Niente da fixare.** Bundle prod: oltre a dice-scene, `index` 548KB/183KB gz; nessuna pagina fuori scala.
  Perf score: con il polling a 0 re-render idle e DiceScene/route verificati sani, Perf sale a **4/4 sul misurabile da browser**; il residuo device (FPS 3D reali) resta annotato sotto.
- ✅ **R3 — Smoke su device** (Android/S22, Telegram in FULLSCREEN) · Esiti raccolti con l'utente:
  ✅ **telegramConfirm**: popup nativo showPopup su elimina personaggio / termina sessione / termina combattimento, conferma e annulla corretti.
  ✅ **VoiceRecorder**: permesso mic richiesto correttamente, registrazione e riascolto funzionanti.
  ❌ **BackButton vs overlay**: in fullscreen NON funziona. Non c'è l'header Telegram (niente freccia); il back di sistema Android (dopo lo swipe che rivela la barra) viene intercettato da Telegram e **minimizza la Mini App** con l'overlay ancora aperto: la sentinella history di `useOverlayDismiss` non riceve mai il popstate. In browser il giro back funziona (verificato B1-B15); su device fullscreen l'unica chiusura resta X/tap-fuori (che restano disponibili: nessun trap, nessuna perdita dati, gravità media). **Bug funzionale segnalato, NON fixato qui (fuori scope della sessione)** — direzione del fix annotata in Fuori scope.

## Giro homebrew fasi 6-7 (sessione 2026-06-15, branch `feat/impeccable-homebrew-fasi67`)

Le fasi 6-7 dell'audit homebrew (PR #178-#180, v2.16.x) hanno introdotto superfici FE nuove DOPO il pass B14, mai verificate visivamente (la memoria di progetto lo segnalava come unica cosa rimasta). Audit FE mirato (`fe-playwright-audit/reports/2026-06-15_01-01-mobile/`) → 4 findings di design (fixati) + 1 funzionale (Fuori scope, #FN1).

- ✅ **HB-r — Homebrew fasi 6-7** · `ResourcesSection`+`ResourceFormModal` (nuovi), `PropertyFormModal` (selettore tono #47), `CustomConditionCard`, `ui/FilterChip`
  Esito: 🔴 i18n `homebrew.resources.*` MANCANTE in it+en (sezione Risorse + intero ResourceFormModal in chiavi grezze, ChipSelect restoration accavallati per il testo lungo) → aggiunto il namespace completo (15 chiavi modal + 4 top, wording allineato alle sezioni sorelle, tra `passive` e `triggers`); modale tradotto, chip recupero non più accavallati. 🟡 touch target dei link "Modifica/override chiave" 17px → classe `hit-44` (PropertyFormModal + ResourceFormModal). 🟡 selettore "Tono badge per valore" non anticipava il colore (chip selected tutti oro identici) → `FilterChip` esteso con toni `danger`/`success` (fill semantico crimson/emerald, testo colorato anche idle, niente halo; refactor del ternario in mappa `TONE_STYLES`); l'editor ora combacia col PropertyBadge a runtime (es. Integro=emerald). 🟡 sottotitolo `CustomConditionCard` troncato a 375 → wrap (rimosso `truncate`, 10→11px, tracking 0.18→0.1em, leading-tight). Verificato 375×667 (+320): zero chiavi grezze (grep+browser), `hit-44` attivo, chip negativo=crimson/positivo=emerald (colori misurati), tsc/lint/console verdi, build:prod ok. Conformi (non toccati): PropertyBadge a runtime, validazione inline tabelle, EffectChainEditor/ActionPicker, hub. Fixture char 9 ripristinata.

## Fuori scope (bug funzionali trovati durante il pass — segnalare, non fixare qui)

- ☐ **DELETE di una regola homebrew di tipo condizione azzera TUTTE le condizioni del personaggio** (audit 2026-06-15): `DELETE …/homebrew/rules/{id}` su una regola che ha materializzato una condizione custom resetta l'intero dict `conditions` (persi `poisoned`/`exhaustion` preesistenti) e lascia la condizione custom orfana (punta alla rule cancellata, va rimossa a mano dalla pagina Condizioni). Perdita dati di gioco silenziosa. Sospetto nel cleanup del ramo `DELETE rule` in `api/services/homebrew` / `api/routers/homebrew.py`. Approfondire con `/fullstack-functional-audit`. **Non di design — non fixato in questo giro.**
- ☐ **Back di sistema non chiude gli overlay su Telegram Android fullscreen** (smoke R3, 2026-06-12): `useOverlayDismiss` integra solo la sentinella history (popstate), che Telegram in fullscreen non riceve mai — il back di sistema minimizza la Mini App con l'overlay aperto. Direzione del fix: integrare la **BackButton API di Telegram** nello stesso stack del hook (`BackButton.show()` quando lo stack non è vuoto, `onClick` → chiudi il top, `hide()` a stack vuoto): per documentazione Telegram, con la BackButton visibile il back hardware Android scatena `back_button_pressed` invece di chiudere/minimizzare. La sentinella history resta per il browser. Un solo punto di fix (il hook copre tutti gli overlay dell'app).
- ✅ ~~**Stabilizza assegna 1 HP**~~ — RISOLTO su `fix/death-saves-raw` (v2.14.5): STABILIZE non cura più, il personaggio resta a 0 HP/stabile (RAW). Test: `test_stabilize_keeps_hp_at_zero`.
- ✅ ~~**`stable` non si resetta col danno 1→0**~~ — RISOLTO su `fix/death-saves-raw` (v2.14.5): la caduta a 0 da HP positivi azzera sempre il tracker (anche `stable` stantio) e il danno a creatura stabile a 0 riapre la morte con conteggio fresco. Test: `test_drop_to_zero_clears_stale_stable`, `test_damage_while_stable_at_zero_restarts_dying_fresh`.

## Diario

- 2026-06-11 · B0 consegnato in PR #166 (v2.14.2) · baseline 14/20
- 2026-06-11 · B1 completato su `feat/impeccable-pass-r2`: hook `useOverlayDismiss` (ESC+back per tutti gli overlay primitivi), conferma-a-destra in HitDiceModal/MapUploadForm/Notes (→ConfirmSheet), X Sheet 40px, label "Riposa". Pendenze: BackButton su device; overlay custom nei batch B4/B5/B10/B11.
- 2026-06-11 · B2 completato: sweep em dash (locale+tsx), description raw soppressa nei saves, `lib/format.ts` per numeri/orari sul locale app (XP, feed 24h).
- 2026-06-11 · B3 completato: guard 404 condiviso nel Layout (EmptyState + CTA), badge "Stabilizzato · privo di sensi" a 0 HP. 2 sospetti funzionali su stabilize → Fuori scope.
- 2026-06-12 · B4 completato su `feat/impeccable-pass-r3`: ricerca indicizza risorse homebrew (#11), header hub compatto col nome a +46% (#V1), ProgressionFullTableModal con ESC/back e tabella ripulita, error state CharacterMain a EmptyState, SwiperDots 40px. Riparati 3 file di test rotti dal giro r2 (mock ApiError/useDragControls/QueryClient).
- 2026-06-12 · B5 completato: overlay equip (action sheet/picker/dettagli) con ESC+back, dettagli oggetto rifiniti (X 40px, numeri mono), tipo oggetto tradotto nel picker, fix warning chiavi duplicate. Tutti i ⛔ del batch esercitati.
- 2026-06-12 · B6 completato + FIX DI SISTEMA: alpha sui token colore (color-mix nel config + sweep 55 arbitrary in 23 file) — 312 tinte del design ora reali in entrambi i temi. i18n banner critico/fumble e CD, ban #fff negli stati HP, #V3 chiuso in radice. Verificati vivo/morente/stabilizzato, dark+light.
- 2026-06-12 · B7 completato: negativo crimson nei Saves, ts_short → "comp", long-press/picker/tiri esercitati; pagine già in gran parte conformi.
- 2026-06-12 · B8 completato su `feat/impeccable-pass-r4`: conferme sulle azioni distruttive (#9 Dimentica + rimozione livello slot), SpellDamageSheet esercitato e portato a sistema (Button primary/Input/SwitchToggle, via text-black), ban #fff nella variante arcane di Button, placeholder e aria i18n, sweep token arbitrary.
- 2026-06-12 · B9 completato: 2 footer invertiti (delete oggetto → ConfirmSheet, convert valuta), Converti de-arcanizzato (Semantic Triad), editor effetti/modificatori esercitati e rifiniti (focus, touch target, i18n), SwitchToggle furtività.
- 2026-06-12 · B10 completato: ESC/back su EditClassesModal (stack annidato col picker) e LevelUpModal, AddClassForm morto eliminato, footer wizard abilità raddrizzato, picker de-arcanizzato con ui/Input, "Liv" i18n, SwitchToggle Passiva + detail modal esercitato, token/touch target su banner e contatori.
- 2026-06-12 · B11 completato su `feat/impeccable-pass-r5` (v2.14.7): fork legacy Card/DndButton/DndInput estinto (+StubPage), conferma-svuota cronologia raddrizzata, viewer mappe con ESC/back e z-index a sistema, fix centratura ZoomableImage, icon button 44px sulle note, mic spiegato via toast, ban #fff su ripple Button e pip spossatezza. VoiceRecorder su device ancora pendente.
- 2026-06-12 · B12 completato: badge FAB parchment (nota B8), gradienti FAB/Lancia → token gradient-gold, ConfirmSheet sui tiri, "Totale"/date i18n, tick d100 sfoltiti, nota B4 (FAB a 320) chiusa come mitigata.
- 2026-06-12 · B13 completato: bug i18n Fisicità/Cultura (solo defaultValue it), banner unsaved da ember a ambra (Semantic Triad), sweep defaultValue+token su Identity/Settings/CharacterSelect, aria ChipInput i18n, glow SwitchToggle a token, ombra warm sul D20Thumb. telegramConfirm lasciato come pattern nativo (valutare in B15).
- 2026-06-12 · B14 completato su `feat/impeccable-pass-r6` (v2.14.8): bug i18n locale nelle liste dell'editor homebrew (sempre etichette IT → resolveLabelI18n), aria EN → i18n, touch target 44/hit-44, FilterPicker a sistema (Input+FilterChip), SwitchToggle sul critico, EmptyState+Riprova, titolo "Nuovo passo" reale. Tutti i ⛔ esercitati (form modali, PropertyBadge, CustomConditionCard, breakdown CA). Gotcha: Vite su /mnt/c senza file-event → riavviare dopo ogni edit.
- 2026-06-12 · B15 completato: REGRESSIONE B3 corretta (guard Layout oscurava la stanza sessione ai giocatori), harden Session.tsx (crash participants + ruolo sempre GM), 3 sheet one-off → ui/Sheet, GrantItemModal a sistema, RewardPopup/feed fullscreen su useOverlayDismiss, sweep 36 token, telegramConfirm benedetto. Combat ⛔ chiuso live con 2 tab dev_user (incontro full end-to-end). Vitest FULL 147/696 verde.
- 2026-06-12 · B16 completato — GIRO E CODA CHIUSI a 19/20 (+5 dalla baseline): sweep finale 49 arbitrary var() in 21 file + ultimo text-black, ban a zero verificati a grep. Residui al futuro: divergenze da benedire in `$impeccable document`, verifiche device, profiling Perf.
- 2026-06-12 · Residui R1-R3 chiusi su `chore/design-pass-residui` (v2.14.9): `document` benedice le 3 divergenze (Illuminated Capital, elastic con confini, filter pill + halo di selezione), giro perf DA MISURE (SessionRoom 24→0 commit idle/min via stripping di `last_activity_at` nel queryFn; DiceScene e route sane, niente altri fix), smoke device (popup nativi ✅, mic ✅, back fullscreen ❌ → Fuori scope con direzione fix BackButton API). Score 19/20 a composizione nuova: Perf 4, Acc 3.
- 2026-06-15 · Giro homebrew fasi 6-7 su `feat/impeccable-homebrew-fasi67` (v2.16.2): audit FE mirato sulle superfici nuove delle fasi 6-7 (mai viste da B14) → 4 fix design — i18n `homebrew.resources.*` mancante (it+en), `hit-44` sui link chiave, `FilterChip` toni danger/success per il selettore tono (allineato al PropertyBadge), wrap subtitle `CustomConditionCard`. Bug funzionale #FN1 (DELETE regola-condizione azzera le condizioni) → Fuori scope.
